SRC=		2048.PAS
UNITS=		ADDTILE.PAS GRID.PAS KEYMAP.PAS MOTION.PAS STATE.PAS STAGES.PAS
BIN=		2048
COMPILER=	fpc
CFLAGS=		-Mtp

${BIN}: ${SRC} ${UNITS}
	${COMPILER} ${CFLAGS} ${SRC}

clean:
	rm -f ${BIN} *.o *.ppu *.tpu
